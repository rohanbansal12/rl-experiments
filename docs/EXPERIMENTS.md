# Experiment Notes

Every committed experiment report should include:

- project
- algorithm
- config path
- seed or seed sweep
- git commit hash
- hardware
- total environment/model steps
- evaluation protocol
- key plots
- failure cases
- interpretation

Store raw local runs in `experiments/runs/` and selected writeups in `experiments/reports/`.
